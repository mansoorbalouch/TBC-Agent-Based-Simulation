import pkg.Agent.*
import pkg.Token.*
import pkg.Platform.*

simSteps = 200;
numAgents = 100;

buyFunc = @(s) 2*s;

for t=1:simSteps
%     select the active agents for t (numAgents = 50)
    numActAgents = 45
    for a=1:numActAgents
        if a == "Fundy"
            fundPrice;
            if (fund)
            return
        elseif a =="Charty"
            return
        elseif a == "Noise"
            return
        end
    end
end
           
mint(100, buyFunc, 20, 50)
